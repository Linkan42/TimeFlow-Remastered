#for deploying login microservice
#
#name: Deploy login microservice
#
#on: 
#
#  push:
#    branches:
#      - main
#    paths:
#      - 'login/**'
#  
#  workflow_dispatch:
#
#jobs:
#  login-microservice:
#    runs-on: ubuntu-latest
#    defaults:
#      run:
#        working-directory: 'login'
#
#    env:
#      #need to fix github secrets
#    
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-node@v4
#        with:
#          node-version: #what node version?
#          cache: 'npm'
#          cache-dependency-path: 'login/package-lock.json'
#      
#      #testing?
#
#      - name: Build
#        #run docker build but first github secrets
#      
#      - name: Publish
#        run: #docker push bit first github secrets
#
#      - uses: tale/kubectl-action@v1
#        with:
#          base64-kube-config: #kubeconfig github secrets
#          kubectl-version: v1.24.2
#
#      
#      - name: Deploy
#        run: envsubst < kubernetes/deploy.yaml | kubectl apply -f -